# Ch4, the hole as absence — 2026-09-21 12:44

Window `9c3b822..cbcaaf3`, read cold. One substantive commit (`05bb5e6`), one
Archivist self-correction (`ac1df0c`), one in-flight scaffold (`496d4bb`), session
logs. The Part-plate round is outside the window and was not reviewed.

## The fix itself: verified, and it holds

Checked independently of the Publisher's summary. `plate_check.py runs/ch04/plate.svg
--chapter 4` returns eleven `ok` rows, no FAIL, no WARN; captions are 5 against a cap
of 5. Fifteen circles changed from `fill=currentColor r=4` to `fill=#ffffff
stroke-width=1.8 r=6.5`. Nothing else moved. Rendered at 3x and at 0.6 scale, the
rings hold their white centres and read as openings; the nails in row one still
visibly pierce the board while rows two and three do not. The author's correction is
now in the marks and not only in the captions.

The Ch5 check was right. `runs/ch05/plate.svg` contains zero `<circle>` elements.
There was nothing to sync.

**The ten is chapter-supported.** Confirmed against the sources, not the desk's
report: `distillation.md:6` — *"the accumulated holes become contempt over time"*;
`refined.md:89` — *"You get there from hundreds of small ones that were never quite
fully repaired."* Row three is the accumulation the prose argues. I agree with the
Designer.

## What was missing: the desk escalated a decision a grep would have settled

The Designer took the picture/caption mismatch in row three to the author with one
option — the Panel's *"The holes remain. And they keep coming."* — correctly noting
it is ungrounded and needs an Author addition. Measured, it WARNs:

```
[WARN] grounded  no three-word run of these appears in the chapter, its
                 distillation or plate-brief.md: 'The holes remain. And they keep coming.'
```

The chapter already contains a sentence that does the job. `refined.md:73` is
**"It's full of holes."** Substituted into the plate and re-checked:

```
[ok] grounded  every run of three or more words is the chapter's
[ok] captions  5 italic lines against a cap of 5 (3 labels + subtitle + closing line)
```

Plural, about accumulation, matches what the picture claims, at the cap, and the
author's own words. The desk reached for the phrase it remembered from Round 2's
Panel rather than searching the chapter it was already holding. That is the finding —
not the caption, which is his call either way.

## What broke, and what recurs: a claim about a file nobody opened

Round 7's notes assert, as the one item needing a decision:

> Round 2 recorded a promise that Ch04 and Ch05 draw one hole glyph exactly [...]
> and Ch05 still draws the old filled dot.

Ch05 has no circles. The claim is false, and the file already said so — 195 lines
earlier, in the same document, an earlier round recorded *"Ch05 no longer draws nails
at all."* The desk did not open `runs/ch05/plate.svg`; it read a prior round's promise
and restated it as current state.

This is not "notes outliving the thing they describe." Notes are append-only by
design and that is fine. The defect is narrower: **a desk asserted the present
contents of a file it never read.** One command would have answered it.

It is the same shape as the error `ac1df0c` corrected in this same window — the
raster staleness claim, "verified by hash", where the hash compared a 3x render
against a 2x file and so could not have said anything but *stale*. Both are a
plausible result, no error raised, never run against the known-right answer. That is
`FINDINGS.md:316`, the seven-defect shape, and the standing instruction.

Two of three of this window's errors are it, and the third — #081, the corpus
finding the prior Archivist "would not have looked for" — is the same thing once
more: `plate_check`'s corpus contents were assumed rather than enumerated.

**The shape, said plainly:** the misses are all claims about the state of another
artifact, made from a prior note or from a check whose parameters were never
confirmed to match. Not reasoning errors. Unopened files.

The correction is currently only in `05bb5e6`'s commit message. The working file
`runs/ch04/plate-notes.md` still carries the false sentence today, and that file, not
`git log`, is what the next plate round reads.

## What worked

- Rule 7 held. The Publisher re-ran `plate_check` rather than accepting the desk's
  report, and independently checked Ch05 before acting on the sync recommendation.
  That catch is the reason this window has a finding instead of a defect.
- `ac1df0c` is an agent correcting its own prior record with evidence and filing the
  mechanical fix (#082) rather than a promise to be careful. Load-bearing; do not
  simplify the self-correction habit away.
- The Designer rejecting the physically-correct channel glyph *on the render* rather
  than on principle, and recording the rejection so it is not re-attempted, is the
  right unit of memory.

## Looked at and found clean

- `alignment` on Ch4: investigated a suspected false `ok`. The mirror clause at
  `plate_check.py:387` does excuse blocks using a partner on the same side or dead
  centre. Measured across all 14 plates: 18 exempt blocks, 14 genuine opposite-side
  mirrors, 4 not. But Ch3's is a deliberate label-left/column-right layout, so
  tightening the clause buys one true positive on Ch4 and costs one false positive on
  Ch3. **Not proposed.** Recorded so the next reader does not re-derive it.
- Row three keeps 4 of the 5 original nail positions, not 5 (258 has no hole). The
  Designer found this, documented why re-spacing fails, and left it. Correct call.
- Corpus: 18,540 words (17,017 on 2026-09-19). Growing, but nothing this window
  proposes adds to it.
- `496d4bb` touches only `runs/`, nothing under `books/`. Rule 8 intact.
- No duplicate of #048, #065, #070-#083.
